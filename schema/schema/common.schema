package gloam.schema;

type Unit {}

// Canonical position component. Required for all entities. Used for load-balancing, spatial finds,
// the inspector, and so on.
component CanonicalPosition {
  id = 830;
  EntityPosition coords = 1;
}

